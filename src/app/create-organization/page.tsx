import { CreateOrganizationView } from "@/features/organizations/CreateOrganizationView";

export const metadata = { title: "Create organization — NDI Studio" };

export default function CreateOrganizationPage() {
  return <CreateOrganizationView />;
}
