import { CreateDidView } from "@/features/wallet/CreateDidView";

export const metadata = { title: "Create DID — NDI Studio" };

export default function CreateDidPage() {
  return <CreateDidView />;
}
