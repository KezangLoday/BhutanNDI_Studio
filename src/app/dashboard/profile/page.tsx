import { OrgProfileView } from "@/features/organizations/OrgProfileView";

export const metadata = { title: "Organization profile — NDI Studio" };

export default function OrgProfilePage() {
  return <OrgProfileView />;
}
