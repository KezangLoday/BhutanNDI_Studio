import { VerificationLedgerView } from "@/features/verification/VerificationLedgerView";

export const metadata = { title: "Verification — NDI Studio" };

export default function VerificationPage() {
  return <VerificationLedgerView />;
}
