import { VerifyMethodView } from "@/features/verification/VerifyMethodView";

export const metadata = { title: "Request a proof — NDI Studio" };

export default function VerifyCredentialsPage() {
  return <VerifyMethodView />;
}
