import { ConnectionPickerView } from "@/features/credentials/ConnectionPickerView";

export const metadata = { title: "Choose a connection — NDI Studio" };

export default function IssueConnectionsPage() {
  return <ConnectionPickerView />;
}
