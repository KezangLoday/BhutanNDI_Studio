import { SchemaSelectionView } from "@/features/credentials/SchemaSelectionView";

export const metadata = { title: "Select a schema — NDI Studio" };

export default function IssueSchemasPage() {
  return <SchemaSelectionView />;
}
