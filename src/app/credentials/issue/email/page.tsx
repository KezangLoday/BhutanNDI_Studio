import { EmailIssuanceView } from "@/features/credentials/EmailIssuanceView";

export const metadata = { title: "Issue by email — NDI Studio" };

export default function EmailIssuancePage() {
  return <EmailIssuanceView />;
}
