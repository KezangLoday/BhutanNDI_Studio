import { BulkHistoryView } from "@/features/credentials/BulkHistoryView";

export const metadata = { title: "Upload history — NDI Studio" };

export default function BulkHistoryPage() {
  return <BulkHistoryView />;
}
