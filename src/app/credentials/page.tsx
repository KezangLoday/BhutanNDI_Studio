import { CredentialsView } from "@/features/credentials/CredentialsView";

export const metadata = { title: "Credentials — NDI Studio" };

export default function CredentialsPage() {
  return <CredentialsView />;
}
