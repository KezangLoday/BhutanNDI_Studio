import { CreateSchemaView } from "@/features/schemas/CreateSchemaView";

export const metadata = { title: "Create schema — NDI Studio" };

export default function CreateSchemaPage() {
  return <CreateSchemaView />;
}
