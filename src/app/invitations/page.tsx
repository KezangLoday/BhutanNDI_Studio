import { InvitationsView } from "@/features/invitations/InvitationsView";

export const metadata = { title: "Invitations — NDI Studio" };

export default function InvitationsPage() {
  return <InvitationsView />;
}
