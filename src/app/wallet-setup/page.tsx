import { WalletSetupView } from "@/features/wallet/WalletSetupView";

export const metadata = { title: "Wallet setup — NDI Studio" };

export default function WalletSetupPage() {
  return <WalletSetupView />;
}
